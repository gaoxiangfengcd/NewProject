# 反代层环境变量。复制为 .env.proxy：
#   cp .env.proxy.example .env.proxy
#
# 二选一：A) Caddy 模式  B) Cloudflare Tunnel 模式
# A) Caddy：填 OFFICIAL_DOMAIN / WEB_DOMAIN / MEMEGO_DOMAIN / ACME_EMAIL
# B) Tunnel：填 CLOUDFLARE_TUNNEL_TOKEN（Caddy 三个变量留空不启动 Caddy）

# ---- A) Caddy 模式 ----
OFFICIAL_DOMAIN=jiandengcun.com
WEB_DOMAIN=hair.example.com
MEMEGO_DOMAIN=memego.jiandengcun.com
ACME_EMAIL=admin@example.com

# ---- B) Cloudflare Tunnel 模式 ----
# Cloudflare Dashboard → Zero Trust → Tunnels → 创建隧道后获取
CLOUDFLARE_TUNNEL_TOKEN=
